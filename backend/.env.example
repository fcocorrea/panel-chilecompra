# Credenciales de la API pública de Mercado Público (https://www.chilecompra.cl/api/)
# Se obtienen registrando nombre, RUT y email en el formulario de la API.
# Copiar este archivo a backend/.env y completar con los valores reales.
# backend/.env está en .gitignore — nunca se sube al repo.
TICKET_API=
USUARIO_API=
